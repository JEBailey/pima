[package]
name = "pima-language-server"
version = "0.1.0"
edition = "2024"

[dependencies]
pima = { path = ".." }
tokio = { version = "1", features = ["io-std", "macros", "rt-multi-thread", "time"] }
tower-lsp = "0.20"
serde_json = "1"

[dev-dependencies]
